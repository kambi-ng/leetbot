import { ThreadChannel } from "discord.js";
import { Meta } from "./interfaces";

function delegate(META: Meta, react: ThreadChannel) {
    // TODO: Call thread handler here
}

export default { delegate };
